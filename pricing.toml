# API prices in USD per million tokens. Last reviewed 2026-09-16.
#
# Prices move often and several of these are promotional, so every entry carries the date it was
# checked and an expiry note where one applies. Re-check before quoting a budget.
#
# "cached_in" is the price for a reused prompt prefix. It matters more here than in most workloads:
# every item from one case repeats that case's presentation and its course so far, so a case's items
# share a long, strictly-growing prefix.

["gemini-3.8-flash"]
input = 0.75
output = 3.75
checked = "2026-09-07"
note = "Introductory. Doubles to $1.50 / $7.50 on 2027-01-01. Output price covers thinking tokens, and this model spends them: a 74-token probe billed 245 total tokens."

["gpt-5.6-luna"]
input = 0.20
output = 1.20
checked = "2026-09-07"
note = "Cut ~80% on 2026-07-30 (was $1.00 / $6.00). 1.1M context. Rejects a custom temperature, so it cannot be pinned to 0.0."

["muse-spark-1.3"]
input = 1.25
output = 4.25
cached_in = 0.15
checked = "2026-09-07"
note = "Meta, standard tier. Use the contributor tier instead for anything derived from published cases; this entry is kept for pricing anything that must not be trained on."

["muse-spark-1.3-contributor"]
input = 0.10
output = 0.20
checked = "2026-09-07"
note = "DEFAULT CHOICE for this project. Meta may train on submitted prompts and completions. That is acceptable here because everything we send is derived from published PMC articles a future model would train on anyway, and because the benchmark is intended for release. The one exception: do not send a split that is meant to stay unpublished as a fresh held-out test set."

["deepseek-v4-flash"]
input = 0.22
output = 0.66
cached_in = 0.007
checked = "2026-09-07"
note = "Off-peak rate; time-of-day pricing began 2026-08-16. Peak is exactly double ($0.014 / $0.44 / $1.32) during 01:00-04:00 and 06:00-10:00 UTC, Monday-Friday — see src/clinical_orchestra/peak.py. Cache hits are ~30x cheaper than misses. Note some pricing pages still show the older flat $0.14 / $0.28 rate, which predates the August change."

["glm-5.3-flash"]
input = 0.15
output = 0.50
cached_in = 0.03
checked = "2026-09-16"
note = "List price. The 50% launch promo ($0.075 / $0.25 / $0.015) ended 2026-09-09 at 24:00 Singapore time; these are the post-promo rates."
