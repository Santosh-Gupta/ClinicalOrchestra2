[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "clinical-orchestra-two"
version = "0.0.1"
description = "A benchmark and harness for predicting the next best step in a clinical case."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Santosh Gupta" }
]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
